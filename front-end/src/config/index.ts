const ADDR = 'http://localhost:8081';


export const configServer = {
    ADDR_HOSTNAME:  ADDR + '/',
    ADDR_GRAPHQL:   ADDR + '/graphql/',
    ADDR_REST:      ADDR + '/api/',
};

export const configEndpoints = {
    ADDR_FACEBOOK_API:  ADDR + '/api/facebook-api/',
    ADDR_AUTH:          ADDR + '/auth/',
};

export const configTheme = {
    DEFAULT_THEME: 'dark',
};